services:
  neo4j:
    image: neo4j:latest
    container_name: mongado-neo4j
    ports:
      - "7474:7474"  # HTTP (browser interface)
      - "7687:7687"  # Bolt protocol
    environment:
      - NEO4J_AUTH=neo4j/mongado-dev-password
      - NEO4J_PLUGINS=["apoc"]
      - NEO4J_dbms_security_procedures_unrestricted=apoc.*
      - BACKUP_DIR=/var/mongado-backups
      - BACKUP_RETENTION_COUNT=14
      - BACKUP_RETENTION_DAYS=30
      # Memory tuning for local dev - keep RAM available for Ollama
      - NEO4J_dbms_memory_heap_initial__size=256m
      - NEO4J_dbms_memory_heap_max__size=512m
      - NEO4J_dbms_memory_pagecache_size=256m
    volumes:
      - neo4j-data:/data
      - neo4j-logs:/logs
      - ./backend/scripts:/scripts:ro  # Backup scripts (read-only)
    networks:
      - mongado-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "cypher-shell", "-u", "neo4j", "-p", "mongado-dev-password", "RETURN 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  ollama:
    image: ollama/ollama:latest
    container_name: mongado-ollama
    ports:
      - "11434:11434"
    environment:
      # Dev-optimized: Keep model loaded for faster iteration, but limit memory usage
      - OLLAMA_MAX_LOADED_MODELS=1  # Only 1 model in memory at a time
      - OLLAMA_NUM_PARALLEL=1        # Limit concurrent requests
      - OLLAMA_KEEP_ALIVE=5m         # Keep model loaded 5min for dev iteration (vs 0 in prod)
    volumes:
      - ollama-data:/root/.ollama
    networks:
      - mongado-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  backend:
    build:
      context: ./backend
      target: development
    container_name: mongado-backend-dev
    ports:
      - "8000:8000"
    volumes:
      # Mount source code for hot reload
      - ./backend:/app
      # Exclude venv to prevent conflicts
      - /app/venv
    environment:
      - DEBUG=true
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=mongado-dev-password
      - OLLAMA_HOST=http://ollama:11434
      - OLLAMA_ENABLED=true
    env_file:
      - ./backend/.env
    networks:
      - mongado-network
    depends_on:
      neo4j:
        condition: service_healthy
      ollama:
        condition: service_started
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      target: development
    container_name: mongado-frontend-dev
    ports:
      - "3000:3000"
    volumes:
      # Mount source code for hot reload
      - ./frontend:/app
      # Exclude node_modules to prevent conflicts
      - /app/node_modules
      - /app/.next
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
      - NODE_ENV=development
    depends_on:
      - backend
    networks:
      - mongado-network
    restart: unless-stopped

networks:
  mongado-network:
    driver: bridge

volumes:
  neo4j-data:
  neo4j-logs:
  ollama-data:
