# Development dependencies (includes base + dev tools)
-r requirements-base.txt

# Testing
pytest==8.4.2
pytest-asyncio==1.2.0
pytest-cov==7.0.0
pytest-mock==3.15.1
pytest-xdist==3.8.0  # Parallel test execution
pytest-benchmark==5.1.0  # Performance benchmarking
httpx==0.28.1  # For testing FastAPI (compatible with ollama)

# Type checking
mypy==1.18.2
types-requests==2.32.4.20250913
types-Pillow==10.2.0.20240423
types-python-dateutil==2.9.0.20251108
types-Pygments==2.19.0.20251121

# Linting and formatting
ruff==0.14.0

# Security
bandit[toml]==1.8.6

# Code quality
radon==6.0.1

# Profiling and debugging
py-spy==0.4.1  # Sampling profiler (no overhead)
memray==1.18.0  # Memory profiler
viztracer==1.0.4  # Tracing profiler with GUI
# line-profiler==4.2.0  # Line-by-line profiler (not yet compatible with Python 3.13 on ARM64)
memory-profiler==0.61.0  # Memory profiler
icecream==2.1.8  # Better print debugging

# Development tools
ipython==9.6.0  # Better REPL
ipdb==0.13.13  # IPython debugger
rich==14.2.0  # Beautiful terminal output
watchfiles==1.1.0  # Fast file watching for hot reload
